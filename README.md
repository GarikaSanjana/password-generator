# 🔐 Password Generator (Python)

A simple Python program that generates strong random passwords using uppercase, lowercase, digits, and special characters.

## 🚀 How to Run
1. Clone the repository
2. Run the script:
   ```bash
   python password_generator.py
